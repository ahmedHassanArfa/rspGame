server:
  servlet:
    context-path: /rspgame
spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/rspGame
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
